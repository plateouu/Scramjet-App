import { IncomingMessage, ServerResponse } from 'http';
import { EventEmitter } from 'events';
declare type ProxyOptions = {
    target: string;
};
declare class ProxyServer extends EventEmitter {
    web(req: IncomingMessage, res: ServerResponse, options: ProxyOptions): EventEmitter;
    delegate(rawRes: ServerResponse, config: any, options: ProxyOptions, emitter: EventEmitter, body?: string): void;
}
export declare const createProxyServer: (opts?: any) => ProxyServer;
export {};
